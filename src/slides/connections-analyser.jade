section
    h3 Criando o analyser
    pre.prettyprint
        code.javascript.
            analyser = audioContext.createAnalyser();
            fbc_array = new Uint8Array(analyser.frequencyBinCount);